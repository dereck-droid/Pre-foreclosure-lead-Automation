# =============================================================================
# ORANGE COUNTY LIS PENDENS SCRAPER - CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Then open .env in a text editor and replace the placeholder values.
# =============================================================================

# --- CAPTCHA SOLVING (2Captcha) ---
# Sign up at https://2captcha.com and add ~$3 to your balance
# Find your API key at: https://2captcha.com/enterpage (after login, top of page)
TWOCAPTCHA_API_KEY=your_2captcha_api_key_here

# --- SKIP TRACING (Tracerfy) ---
# Sign up at https://www.tracerfy.com
# Find your API key in your account settings after login
TRACERFY_API_KEY=your_tracerfy_api_key_here

# --- CRM INTEGRATION ---
# Set CRM_MODE to "webhook" or "gohighlevel"
CRM_MODE=webhook

# If using a generic webhook (simplest option):
CRM_WEBHOOK_URL=https://your-webhook-url-here

# If using GoHighLevel:
GHL_API_KEY=your_gohighlevel_api_key_here
GHL_LOCATION_ID=your_location_id_here

# --- NOTIFICATIONS (Optional - for error alerts) ---
# Twilio SMS alerts (sign up at https://www.twilio.com)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
ALERT_PHONE_NUMBERS=+15551234567,+15559876543

# --- SCRAPER SETTINGS ---
# How many times per day to check (during business hours)
# Options: "twice" (9am + 2pm) or "three" (8am + 12pm + 4pm)
CHECK_FREQUENCY=twice

# Run in visible browser mode for debugging (true/false)
# Set to true when testing, false for production
HEADLESS=false

# Maximum number of results pages to scrape per run (safety limit)
MAX_PAGES=50
